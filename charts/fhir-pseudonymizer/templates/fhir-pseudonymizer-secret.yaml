{{- if and (.Values.auth.apiKey.enabled) (not .Values.auth.apiKey.existingSecret) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "fhir-pseudonymizer.fullname" . }}-fhir-pseudonymizer-api-key
  labels:
    {{- include "fhir-pseudonymizer.labels" . | nindent 4 }}
    app.kubernetes.io/component: fhir-pseudonymizer
type: Opaque
data:
  APIKEY: {{ .Values.auth.apiKey.key | b64enc | quote }}
{{- end -}}
