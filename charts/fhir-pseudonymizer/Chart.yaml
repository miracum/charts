apiVersion: v2
name: fhir-pseudonymizer
description: A Helm chart for deploying the MIRACUM FHIR Pseudonymizer
type: application
sources:
  - https://github.com/miracum/fhir-pseudonymizer
home: https://github.com/miracum/fhir-pseudonymizer
dependencies:
  - name: vfps
    version: 1.2.5
    repository: oci://ghcr.io/miracum/charts
    condition: vfps.enabled
version: 0.4.7
annotations:
  # When using the list of objects option the valid supported kinds are
  # added, changed, deprecated, removed, fixed and security.
  artifacthub.io/changes: |
    - kind: changed
      description: updated curl image to 8.2.0
    - kind: changed
      description: updated fhir-pseudonymizer image to 2.17.1
    - kind: added
      description: allow setting resource limits and requests for the Helm test pods
