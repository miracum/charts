fhir-pseudonymizer:
  pseudonymizationService: "Vfps"
  vfps:
    enabled: true
    postgres:
      enabled: true
      nameOverride: "vfps-postgres"
    resourcesPreset: "micro"

metrics:
  serviceMonitor:
    enabled: true
    additionalLabels:
      release: kube-prom-stack
