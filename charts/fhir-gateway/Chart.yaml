apiVersion: v2
name: fhir-gateway
description: A Helm chart for deploying the MIRACUM FHIR Gateway
type: application
sources:
  - https://github.com/miracum/fhir-gateway
home: https://github.com/miracum/fhir-gateway
dependencies:
  - name: postgresql
    version: 12.1.2
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
version: 5.0.0
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: "updated curlimages/curl image to 7.87.0"
    - kind: changed
      description: "updated fhir-pseudonymizer image to 2.15.1"
    - kind: changed
      description: "⚠️ BREAKING CHANGE: updated postgresql sub-chart to 12.1.2. Uses postgres v15 internally."
