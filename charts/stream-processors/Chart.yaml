apiVersion: v2
name: stream-processors
description: A Helm chart for deploying a set of Kafka stream processing apps
type: application
home: https://gitlab.miracum.org/miracum/etl/streams
annotations:
  artifacthub.io/changes: |
    # When using the list of objects option the valid supported kinds are
    # added, changed, deprecated, removed, fixed and security.
    - kind: changed
      description: bumped version to test changelog generation
version: 1.1.4